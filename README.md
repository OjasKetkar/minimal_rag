# minimal_rag
To build a minimal RAG system to understand memory behaviour

This implemetation serves as a baseline RAG system with static memory behaviour, used for comparison against memory-aware agentic architectures in subsequent work.
